import React from "react";

export const recipesContext = React.createContext();
export const RecipesProvider = recipesContext.Provider;

export const vipRecipesContext = React.createContext();
export const VipRecipesProvider = vipRecipesContext.Provider;

export const updateContext = React.createContext();
export const UpdateProvider = updateContext.Provider;


